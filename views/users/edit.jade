extend ../layout

block content
  form(action="/users/" + user._id + "/update?_method=put", method="post")
    div.input-group
      label.input-group-addon username &nbsp; &nbsp;
      input.form-control(name="username", type="text", value="#{user.username}")

    div.input-group
      label.input-group-addon email &nbsp; &nbsp;
      input.form-control(name="email", type="text",value="#{user.email}")

    div.input-group
      label.input-group-addon password &nbsp; &nbsp;
      input.form-control(name="password", type="password")

    div.input-group
      button 更新

